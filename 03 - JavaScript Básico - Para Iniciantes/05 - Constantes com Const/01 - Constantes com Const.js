// We not declarate a variable with the reserved words of the JS
// ***THE VARIABLES MUST HAVE SIGNIFICANT NAMES***
// Do not start the variable name with a number
// Do not contains spaces " " or "-"
// The convention is to use camelCase
// The JS is Case-sensitive
// We cannot change the value of a constant
const user = "Gustavo";
console.log(user);

// We cannot declares a const without initialize her
// -> const nome;

// String = Text | Number = Number

// To find the type of a variable, use the "typeof()" function
let text = "Some text";
let number = 10;
console.log(typeof(text))
// OR
console.log(typeof number)