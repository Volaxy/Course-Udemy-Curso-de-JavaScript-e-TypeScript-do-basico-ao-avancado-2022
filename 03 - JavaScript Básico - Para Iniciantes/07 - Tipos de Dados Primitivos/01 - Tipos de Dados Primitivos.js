const user = "Gustavo"; // String
const x = 10; // Number
const y = 10.5; // Number
const boolean = true // Boolean = true or false (Logic value)
const aproved = false;

let student; // Undefined = Not pointed anywhere in the memory
let lastName = null // Not pointed anywhere in the memory, we use "null" when we want a variable to have no value, such as the absence of a user's profile background
lastName = "red";

/**
 * The difference between the "undefined" and the "null" is that when we use "null", we are telling the program that we don't want any value assigned to the variable, and "undefined" is assigned by JS.
 */